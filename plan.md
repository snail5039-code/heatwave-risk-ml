# 머신러닝 프로젝트 진행 계획

## 1. 프로젝트 방향

이 프로젝트는 기상 조건을 바탕으로 날짜별 온열질환자 수를 예측하는 것을 목표로 한다.

핵심 분석 방식은 회귀분석이다.

- 예측 대상: 날짜별 온열질환자 수
- 주요 모델: Decision Tree Regressor, Random Forest Regressor
- 결과 활용: 예측된 환자 수를 기준으로 외출 위험도를 단계화

## 2. 사용할 데이터

| 파일 | 역할 | 주요 컬럼 |
| --- | --- | --- |
| `heat_illness_2025_unified.csv` | 온열질환 발생 정답 데이터 | 날짜, 지역, 온열질환자 수, 사망자 수, 연령별, 발생시간별 |
| `OBS_ASOS_DD_20260830192528.csv` | 기상 feature 데이터 | 평균기온, 최저기온, 최고기온, 강수량, 풍속, 습도, 일조시간, 일사량 |

분석 기간은 두 데이터가 함께 존재하는 `2025-05-15 ~ 2025-09-25`로 설정한다.

## 3. 전체 진행 순서

세부 실행 문서는 아래 파일을 참고한다.

| 단계 | 문서 |
| --- | --- |
| Step 1. 데이터 확인 | [steps/01_data_check.md](steps/01_data_check.md) |
| Step 2. 데이터 전처리 | [steps/02_preprocessing.md](steps/02_preprocessing.md) |
| Step 3. 탐색적 데이터 분석 | [steps/03_eda.md](steps/03_eda.md) |
| Step 4. 회귀 모델 학습 | [steps/04_modeling.md](steps/04_modeling.md) |
| Step 5. 외출 위험도 단계화 | [steps/05_risk_level.md](steps/05_risk_level.md) |
| Step 6. Streamlit 앱 구현 | [steps/06_streamlit.md](steps/06_streamlit.md) |

### Step 1. 데이터 확인

- 온열질환 CSV와 기상 CSV를 불러온다.
- 각 파일의 행 수, 컬럼명, 날짜 범위를 확인한다.
- 결측치가 많은 컬럼을 확인한다.
- 분석에 사용할 record_type을 정한다.

우선 사용할 온열질환 데이터는 `daily_national` 또는 `daily_region`이다.

### Step 2. 분석 단위 결정

두 가지 방식 중 하나를 선택한다.

| 방식 | 설명 | 장점 | 단점 |
| --- | --- | --- | --- |
| 전국 날짜별 분석 | 전국 온열질환자 수와 전국/대표 기상값을 사용 | 단순하고 빠르게 진행 가능 | 지역별 차이를 반영하기 어려움 |
| 지역 날짜별 분석 | 날짜와 지역 기준으로 온열질환자 수와 기상 데이터를 병합 | 프로젝트 주제와 더 잘 맞음 | 지역명/관측소 매칭이 필요함 |

이번 프로젝트의 우선 분석 단위는 전국 날짜별 분석으로 설정한다.

지역 날짜별 분석은 이후 확장 방향으로 남긴다.

### Step 3. 데이터 전처리

- 온열질환 데이터에서 날짜별 환자 수를 추출한다.
- 기상 데이터에서 분석 기간에 해당하는 행만 남긴다.
- 날짜 형식을 통일한다.
- 결측치를 처리한다.
- 필요한 기상 변수만 선택한다.

사용할 기본 feature 후보:

- 평균기온
- 최고기온
- 최저기온
- 평균 상대습도
- 평균 풍속
- 일강수량
- 합계 일조시간
- 합계 일사량

target:

- 날짜별 온열질환자 수

### Step 4. 탐색적 데이터 분석

- 날짜별 온열질환자 수 변화를 그래프로 확인한다.
- 기온이 높은 날과 환자 수가 많은 날을 비교한다.
- 연령별 온열질환자 수를 확인해 고령층 비중을 설명한다.
- 발생시간별 온열질환자 수를 확인해 위험 시간대를 설명한다.

### Step 5. 회귀 모델 학습

사용 모델:

1. Decision Tree Regressor
2. Random Forest Regressor

모델 학습 과정:

- train/test 데이터 분리
- Decision Tree Regressor 학습
- Random Forest Regressor 학습
- 예측값과 실제값 비교
- 성능 평가

평가 지표:

- MAE
- RMSE
- R2 Score

### Step 6. Streamlit 서비스 구현

학습한 모델 결과를 사용자가 쉽게 확인할 수 있도록 Streamlit 화면을 만든다.

주요 화면 구성:

- 오늘의 폭염 외출 위험도 요약
- 날짜 또는 지역 선택
- 나이, 외출 예정 시간, 예상 외출시간 입력
- 기상 조건 입력 또는 선택
- 예측 온열질환자 수 표시
- 낮음 / 보통 / 높음 / 매우 높음 위험등급 표시
- 실제값과 예측값 비교 그래프
- 기상 변수 중요도 그래프
- 연령별 및 발생시간별 온열질환 현황 시각화

Streamlit 구현 파일 후보:

- `app.py`: Streamlit 메인 화면
- `model_train.ipynb`: 데이터 분석 및 모델 학습 노트북
- `model.pkl`: 학습된 모델 저장 파일
- `data/`: 전처리된 데이터 저장 폴더

### Step 7. Streamlit 배포

최종 서비스는 Streamlit Community Cloud 또는 로컬 실행 방식으로 배포한다.

배포 시 필요한 파일:

- `app.py`
- `requirements.txt`
- 학습된 모델 파일
- 분석에 필요한 CSV 파일
- README 또는 사용 설명 문서

## 4. 모델 사용 이유

### Decision Tree Regressor

Decision Tree Regressor는 기온, 습도, 풍속, 일조시간 등의 조건을 단계적으로 나누어 온열질환자 수를 예측할 수 있다.

분석 과정이 비교적 직관적이기 때문에 발표에서 설명하기 쉽다.

### Random Forest Regressor

Random Forest Regressor는 여러 개의 Decision Tree를 결합하여 예측하는 모델이다.

단일 Decision Tree보다 과적합을 줄이고 더 안정적인 예측 결과를 얻을 수 있다.

또한 변수 중요도를 확인할 수 있어 평균기온, 최고기온, 습도, 일조시간 중 어떤 기상 요인이 온열질환 발생 예측에 큰 영향을 주는지 설명할 수 있다.

## 5. 외출 위험도 단계화

회귀분석으로 예측한 온열질환자 수를 바로 보여주는 것보다, 사용자가 이해하기 쉬운 위험등급으로 바꿔 제시한다.

예시 기준:

| 예측 온열질환자 수 | 위험등급 |
| --- | --- |
| 0명 | 낮음 |
| 1~5명 | 보통 |
| 6~20명 | 높음 |
| 21명 이상 | 매우 높음 |

위험등급 기준은 실제 온열질환자 수 데이터를 확인한 뒤 설정한다.

초기 기준은 실제 환자 수 분포를 바탕으로 정하고, Streamlit 화면에서는 예측된 환자 수를 해당 기준에 따라 위험등급으로 변환한다.

## 6. 최종 결과물

- 전처리된 학습 데이터셋
- 날짜별 온열질환자 수 예측 모델
- 실제값과 예측값 비교 그래프
- 기상 변수 중요도 그래프
- 고령자 폭염 외출 위험도 단계화 결과
- Streamlit 기반 폭염 외출 위험도 예측 서비스
- Streamlit 실행 화면

## 7. Streamlit 디자인 방향

서비스의 핵심 사용자는 폭염 위험을 빠르게 확인해야 하는 사용자이므로, 화면은 간단한 안전 알림 서비스와 분석 대시보드가 함께 보이도록 구성한다.

### 화면 컨셉

- 공공 안전 서비스 느낌
- 입력형 예측 서비스와 분석 결과 대시보드를 함께 제공
- 한눈에 위험도를 확인할 수 있는 구성
- 고령자도 이해하기 쉬운 큰 글씨와 명확한 색상 사용
- 너무 화려한 디자인보다 정보 전달 중심의 깔끔한 구성

### 색상 기준

| 위험등급 | 색상 | 의미 |
| --- | --- | --- |
| 낮음 | 초록색 | 외출 가능 |
| 보통 | 노란색 | 주의 필요 |
| 높음 | 주황색 | 외출 자제 권고 |
| 매우 높음 | 빨간색 | 외출 위험 |

### 화면 배치

1. 상단
   - 프로젝트 제목
   - 선택한 날짜/지역 정보

2. 왼쪽 입력 영역
   - 날짜 선택
   - 지역 선택
   - 나이 입력
   - 외출 예정 시간 선택
   - 예상 외출시간 입력
   - 평균기온, 최고기온, 습도, 풍속 등 기상 조건 입력
   - 예측 버튼

3. 중앙 결과 영역
   - 예측 온열질환자 수
   - 외출 위험등급
   - 위험도 설명 문구

4. 하단 대시보드 영역
   - 기상 변수 그래프
   - 실제값/예측값 비교 그래프
   - 연령별, 발생시간별 온열질환 현황

### 사용자 입력 요소

- 날짜 선택
- 지역 선택
- 나이 입력
- 외출 예정 시간 선택
- 예상 외출시간 입력
- 평균기온, 최고기온, 습도, 풍속 등 기상 조건 입력
- 예측 버튼

### 분석 대시보드 요소

- 날짜별 온열질환자 수 추이
- 실제값과 예측값 비교
- 기상 변수 중요도
- 연령별 온열질환자 수
- 발생시간별 온열질환자 수

### 결과 문구 예시

```text
예측 온열질환자 수: 24명
외출 위험도: 매우 높음
입력 조건: 78세, 오후 3시 외출 예정, 예상 외출시간 1시간
권고 사항: 고령자는 오후 시간대 외출을 자제하고, 충분한 수분 섭취와 실내 휴식을 권장합니다.
```

## 8. 현재 결정사항

- 분석 단위는 전국 날짜별 분석으로 먼저 진행한다.
- 위험등급 기준은 실제 온열질환자 수 데이터를 바탕으로 설정한다.
- Streamlit 화면은 입력형 예측 서비스와 분석 결과 대시보드를 함께 구성한다.
- 고령자 위험도는 연령별 데이터를 모델에 반영하는 방향으로 검토한다.
- 무더위쉼터 안내 기능은 기본 기능 완성 이후 확장 아이디어로 남긴다.
- 배포는 사용자가 직접 진행하며, 프로젝트에서는 Streamlit 앱 제작까지 진행한다.
