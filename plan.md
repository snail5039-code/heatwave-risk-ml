# 머신러닝 프로젝트 진행 계획

## 1. 프로젝트 목표

2022~2025년의 날짜별 전국 기상 조건을 이용해 전국 일일 온열질환자 수를 예측하고, 예측 결과를 사용자가 이해하기 쉬운 위험등급과 외출 안내로 제공한다.

- 분석 방식: 회귀분석
- 분석 단위: 전국 날짜별
- 최종 모델: Random Forest Regressor
- 서비스 형태: Streamlit 예측 화면과 분석 대시보드

## 2. 현재 데이터

| 데이터 | 기간 | 역할 |
| --- | --- | --- |
| 온열질환자 데이터 | 2022~2025년 감시기간 | 날짜별 전국 환자 수인 target |
| ASOS 일별 기상 데이터 | 2022~2025년 감시기간 | 기온, 강수량, 풍속, 습도, 일조시간, 일사량 feature |

최종 학습 데이터는 `data/processed/train_dataset.csv`이며 총 536일이다.

연도별 환자 수는 2022년 1,564명, 2023년 2,818명, 2024년 3,704명, 2025년 4,460명이다.

## 3. 진행 현황

| 단계 | 상태 | 산출물 또는 결과 |
| --- | --- | --- |
| Step 1. 데이터 확인 | 완료 | 원본 기간, 컬럼, 인코딩, 결측치 확인 |
| Step 2. 데이터 전처리 | 완료 | `heat_illness_daily_2022_2025.csv`, `train_dataset.csv` |
| Step 3. 탐색적 데이터 분석 | 다음 진행 | 환자 수 분포, 기상 관계, 변수 중요도 시각화 |
| Step 4. 회귀 모델 학습 | 완료 | Decision Tree와 Random Forest 비교 및 튜닝 |
| Step 5. 외출 위험도 단계화 | 대기 | 데이터 분포에 근거한 위험등급과 안내 문구 |
| Step 6. Streamlit 앱 구현 | 대기 | 저장 모델을 사용하는 예측 화면과 대시보드 |

## 4. 최종 모델

```python
RandomForestRegressor(
    n_estimators=100,
    max_depth=7,
    min_samples_split=8,
    min_samples_leaf=5,
    max_features="sqrt",
    random_state=42
)
```

### 성능

| 평가 방식 | MAE | RMSE | R² |
| --- | ---: | ---: | ---: |
| 2022~2025년 무작위 80:20 분리 | 9.700 | 14.871 | 0.846 |
| 2022~2024년 학습, 2025년 테스트 | 13.128 | 23.960 | 0.686 |

- 기본 성능은 무작위 테스트 R² 0.846이다.
- 새로운 연도에 대한 일반화 성능은 2025년 테스트 R² 0.686이다.
- R²는 정확도가 아니라 환자 수 변화를 설명하는 정도다.

## 5. 고령자 외출 안내 범위

현재 모델의 출력은 개인의 발병 확률이 아니라 **전국 일일 예상 온열질환자 수**다.

따라서 현재 모델만으로 특정 고령자에게 `외출 가능`을 확정해서는 안 된다. Streamlit에서는 다음 두 결과를 구분한다.

1. 모델 결과: 전국 일일 예상 환자 수와 전국 위험등급
2. 안내 규칙: 나이, 외출 시간대, 외출시간, 기온 등을 반영한 고령자 외출 권고

안내 문구는 `외출 가능` 같은 단정 대신 아래처럼 표현한다.

- 위험 낮음: 비교적 위험이 낮지만 수분 섭취와 휴식 필요
- 주의: 장시간 외출을 피하고 그늘에서 자주 휴식
- 높음: 고령자는 한낮 외출 자제 권고
- 매우 높음: 고령자는 외출 연기 또는 실내 활동 권고

이 안내는 의료적 안전 판정이 아닌 참고용 예방 안내임을 화면에 표시한다.

## 6. 다음 진행 순서

1. EDA로 환자 수 분포와 기상 요소 관계 확인
2. 모델의 기상 변수 중요도 그래프 생성
3. 실제 환자 수 분포를 기준으로 위험등급 경계 결정
4. 고령자 외출 안내 규칙과 문구 결정
5. Streamlit에서 저장 모델 `model/saved/heat_patient_model.pkl` 불러오기
6. 예측 환자 수, 위험등급, 외출 권고, 분석 그래프 구현
7. 전체 기능 검증 후 사용 설명과 README 최신화

## 7. 최종 결과물

- 전처리된 2022~2025년 학습 데이터
- 저장된 랜덤포레스트 회귀 모델
- 실제값과 예측값 비교 그래프
- 기상 변수 중요도 그래프
- 전국 위험등급과 고령자 외출 권고 규칙
- Streamlit 기반 예측 서비스

